function ball() {
    var sun = document.getElementById('ball');
    sun.style.visibility = "visible";
    sun.style.animation = "ball 5s ease-in-out infinite";
    var btn = document.getElementById('btn');
    btn.style.visibility = "hidden";
}